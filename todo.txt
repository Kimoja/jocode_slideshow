/*
 * TODO
 * tester evenement ontouch
 * evenement mouseweheel
 * ajouter des descriptions notamment  pour les images, ou le faire via les méthodes afterDraw ou beforeDraw?
 * fx, changer la taille du conteneur par rapport au contenue, en option , autowidth, autoheight, par d?faut dans la classe de base fx ???  !important
 * activer l'historique de navigation au changement de slide .... pour des sites avec juste un slideshow comme contenue, ca peut ?tre pas mal
 * ?couteur live on click sur un slide, !important 
 * option plein ?cran .... faut que  l'int?gration HTML et CSS le permet, en tout cas via un plugin, ? r?fl?chir ...
 * tout plein de transition qui envoie
 * 
 * 
    *      @param {Boolean}    [config.display_bt_on_load=true]        ???
    *      @param {Boolean}    [config.display_bt_on_fx=true]          ???
    *      @param {Boolean}    [config.display_nav_on_load=true]        ???
    *      @param {Boolean}    [config.display_nav_on_fx=true]          ???
 **/




$.JocodeSlideshowProgress = {
    
    /**
     * TODO
     * 
     * @constructor 
     * @name $.JocodeProgressBar.Base
     **/
    Base : $.jocodeClass(
        function(){
            
        }, 
        {
            /** @lends $.JocodeProgressBar.Base.prototype */

            /**
             * 
             * @function
             * @param {Number} value
             * @param {Number} delay
             */
            progress : function(value){}
        }
    )
};

